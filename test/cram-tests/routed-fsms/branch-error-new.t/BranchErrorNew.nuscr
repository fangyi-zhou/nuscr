(*# CheckDirectedChoiceDisabled #*)

global protocol BranchErrorNew(role C, role A, role S) {
    a() from C to A;
    choice at C {
        b() from C to A;
        b() from A to S;
    } or {
        c() from C to A;
    }
}
